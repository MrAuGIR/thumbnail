# Loaded by ThumbnailExtension::load() only when Twig is installed (optional dependency).
services:
    MrAuGir\Thumbnail\Twig\TwigThumbnailExtension:
        arguments:
            $urlGenerator: '@router'
        tags: ['twig.extension']
